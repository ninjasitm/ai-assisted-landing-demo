---
description: Development environment and tooling standards
globs:
alwaysApply: agent
---

- Tailor commands to the user's OS and shell.
- Use `npm` for package management (tooling only; no runtime dependencies).
- Use Live Server (VS Code extension) for local development on port 5500.
- Use `npx tailwindcss` CLI for building and watching CSS.
